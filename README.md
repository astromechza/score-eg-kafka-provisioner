# score-eg-kafka-provisioner

This repo is used to demonstrate a custom kafka-topic provisioner.

```bash
$ brew install score-spec/tap/score-compose
$ score-compose init
$ score-compose generate score.yaml --build main=.
$ docker compose up
```
